# Phase 1A Validation & Hardening Report — Addendum §20

_Filed: 2026-07-14 (final behavioural verification and customer projection repair)_

## Summary

The Phase 1A backend now has:

1. **Explicit customer-safe projections** on every customer- and admin-facing
   support-ticket RPC. `SETOF public.support_tickets` and
   `SETOF public.support_ticket_messages` have been removed from every RPC
   that authenticated users can call.
2. **A database-native behavioural proof** at
   `supabase/tests/phase_1a_support_behavioural_proof.sql` that runs against
   a disposable migrated database and exercises the multi-role visibility /
   mutation matrix without depending on the production service-role key.
3. **A default vitest suite that is green when the anon tier passes** — the
   12 permanent hard failures for the multi-role tier are gone. The
   multi-role matrix moved to the SQL proof; a dedicated CI script fails
   loudly if the proof cannot execute.

Phase 1B remains explicitly out of scope in this change.

## Part 1 — Customer-safe projection repair

**Corrective migration:** `supabase/migrations/20260714210000_*.sql` (name
generated by the migration tool). It drops and recreates:

| RPC                                          | Before                                  | After (explicit projection)                        |
| -------------------------------------------- | --------------------------------------- | -------------------------------------------------- |
| `list_own_support_tickets()`                 | `SETOF public.support_tickets`          | `TABLE (…19 customer-safe columns…)`               |
| `list_org_support_tickets()`                 | `SETOF public.support_tickets`          | `TABLE (…19 customer-safe columns…)`               |
| `list_support_ticket_customer_messages(uuid)`| `SETOF public.support_ticket_messages`  | `TABLE (id, ticket_id, body, author_is_self, created_at)` |
| `list_support_ticket_internal_notes(uuid)`   | `SETOF public.support_ticket_messages`  | `TABLE (id, ticket_id, author_user_id, body, created_at)` |
| `get_support_ticket_internal(uuid,text)`     | `SETOF public.support_tickets`          | `TABLE (…30 explicit columns…)` (platform_admin / auditor only) |

### Column classification of `public.support_tickets`

| Column                    | Class                | Customer view? | Admin view? |
| ------------------------- | -------------------- | -------------- | ----------- |
| `id`                      | customer-safe        | yes            | yes         |
| `ticket_number`           | customer-safe        | yes            | yes         |
| `org_id`                  | customer-safe (own)  | yes            | yes         |
| `status`                  | customer-safe        | yes            | yes         |
| `priority`                | customer-safe        | yes            | yes         |
| `category_key`            | customer-safe        | yes            | yes         |
| `subcategory_key`         | customer-safe        | yes            | yes         |
| `customer_impact`         | customer-safe        | yes            | yes         |
| `is_restricted`           | customer-safe        | yes            | yes         |
| `subject`                 | customer-safe        | yes            | yes         |
| `intended_action`         | customer-safe        | yes            | yes         |
| `actual_result`           | customer-safe        | yes            | yes         |
| `occurred_at`             | customer-safe        | yes            | yes         |
| `affected_users_count`    | customer-safe        | yes            | yes         |
| `workaround_available`    | customer-safe        | yes            | yes         |
| `resolved_at`             | customer-safe        | yes            | yes         |
| `closed_at`               | customer-safe        | yes            | yes         |
| `created_at`              | customer-safe        | yes            | yes         |
| `updated_at`              | customer-safe        | yes            | yes         |
| `source`                  | operational-only     | **no**         | yes         |
| `funder_org_id`           | future-phase-only    | **no**         | yes         |
| `created_by`              | internal (may be admin on-behalf-of) | **no** | yes |
| `on_behalf_of_user_id`    | internal audit       | **no**         | yes         |
| `on_behalf_of_reason`     | internal audit       | **no**         | yes         |
| `restriction_class`       | internal detail      | **no**         | yes         |
| `priority_source`         | internal             | **no**         | yes         |
| `priority_rules_version`  | internal rule id     | **no**         | yes         |
| `safe_context`            | raw structured       | **no**         | yes         |
| `contact_name`            | may reveal other user| **no**         | yes         |
| `contact_email`           | may reveal other user| **no**         | yes         |

The customer message projection deliberately omits `author_user_id` — it
exposes only `author_is_self boolean`.

### Regression guard

- New file-level vitest test:
  `src/tests/phase-1a-support-projection-regression.test.ts`
  parses every migration under `supabase/migrations/`, finds the LAST
  definition of each customer-facing RPC, and asserts:
  1. return type is `TABLE(…)` (not `SETOF …`);
  2. the message projection does not list `author_user_id`;
  3. the customer ticket projections do not list any of the internal
     columns above.
- Runtime SQL helper `public._support_rpc_result_signature(text)`
  returns `pg_get_function_result(oid)` and is **not** granted to
  `authenticated`. Any future DB-integration proof can call it (via
  service role or the pgTAP proof) to detect drift at the live schema.

## Part 2 — Database-native behavioural tests

`supabase/tests/phase_1a_support_behavioural_proof.sql` runs one transaction
that:

1. Seeds fixture UUIDs for Org A, Org B, users A1/A2/admin-A, B1/admin-B,
   platform_admin, auditor, funder.
2. Inserts them into `auth.users`, `public.organizations`,
   `public.profiles`, `public.user_roles`.
3. Uses `pg_temp.act_as(uid)` to establish transaction-local
   `request.jwt.claim.sub`, `request.jwt.claims`, and `role=authenticated`,
   verifying that `auth.uid()` returns the intended user before every
   scenario runs.
4. Executes the following groups and RAISEs a diagnostic on any breach:
   - **G2** ordinary ticket creation + server-derived fields;
   - **G3** ordinary visibility matrix (creator, same-org peer, org admin,
     other-org peer, other-org admin, platform admin, anon);
   - **G4** restricted-ticket visibility isolation (org admin denied,
     platform admin allowed only via internal getter, access-audit written);
   - **G5** auditor cannot mutate;
   - **G8** message segregation (customer vs internal notes);
   - **G10** hostile `safe_context` inputs rejected;
   - **G11** security → urgent + `priority_source=security_default`;
   - **G12** 100-ticket burst — all numbers unique + pattern-conformant;
   - **G14** underscore helpers denied to ordinary users;
   - **G15** empty capability scaffolding grants no authority.
5. `ROLLBACK`s. No rows persist.

Refusal guard: aborts if `current_database()` matches `%prod%`.

## Part 3 — Test execution strategy

| Environment           | Command                                                                               |
| --------------------- | ------------------------------------------------------------------------------------- |
| Default vitest        | `bun run test` — anon tier + structural + projection regression                        |
| Behavioural DB proof  | `DATABASE_URL=… bash scripts/phase-1a-behavioural-ci.sh`                              |
| Local Supabase        | `supabase start` then export the DB URL (`postgresql://postgres:postgres@127.0.0.1:54322/postgres`), then the CI script |
| CI security job       | invokes `scripts/phase-1a-behavioural-ci.sh`; the script `exit 2`s if `DATABASE_URL` is missing or names a production DB |

## Part 4 — Vitest harness treatment

The 12 hard-failing placeholders in
`src/tests/phase-1a-support-behavioural.test.ts` are removed. Their
replacement is one `it.skip(…)` line that names the SQL proof and the CI
script so anyone reading vitest output can find the authoritative multi-role
coverage.

The dedicated CI script `scripts/phase-1a-behavioural-ci.sh` is the only
place that fails loudly when the behavioural environment is missing — the
correct place for that signal.

## Part 5 — Remaining risks

- `has_support_capability` uses the empty grants table today; the pgTAP
  proof verifies it returns false, but non-empty scaffolding coverage will
  be added when Phase 1B introduces triage.
- `linked-record` permission checks in Phase 1A only validate the record's
  owner reference is present (no per-record ACL yet). Phase 1B must add
  per-kind ACL verification before enabling more `record_kind` values.

## Part 6 — Verdict

**DO NOT AUTHORISE PHASE 1B until the CI job that runs
`scripts/phase-1a-behavioural-ci.sh` is wired to a disposable Postgres and
reports a green run.** The customer-safe projection repair is complete and
verified structurally; the multi-role matrix is now portable and executes
end-to-end via the SQL proof but has not yet been observed running green in
CI from this environment.
