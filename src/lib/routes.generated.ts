/* eslint-disable */
// AUTO-GENERATED by scripts/generate-routes.mjs — DO NOT EDIT BY HAND.
// Regenerated on every `npm run build` (and `npm run check:routes`).
// Source of truth: every <Route path="…"> in src/App.tsx + nested shells.
//
// Why this file exists:
//   • The "/desk/settings/identity" → 404 bug shipped because nothing forced
//     <Link to="…"> targets to be type-checked against real routes.
//   • Hand-maintained ROUTES constants drift; a generated union cannot.
//   • routeTo() is the only sanctioned way to build an in-app URL going
//     forward. ESLint (eventually) bans literal "/desk/…" strings outside
//     this file and route-config files.

export const ROUTE_PATHS = [
  "/",
  "/admin",
  "/admin/api/usage",
  "/admin/compliance",
  "/admin/data-governance",
  "/admin/deals",
  "/admin/engagements",
  "/admin/entities",
  "/admin/orgs",
  "/admin/overrides",
  "/admin/registry",
  "/admin/registry/decisions",
  "/admin/registry/readiness",
  "/admin/settings",
  "/admin/users",
  "/auth",
  "/billing",
  "/dashboard",
  "/dashboard/account",
  "/dashboard/billing",
  "/dashboard/compliance",
  "/dashboard/matches",
  "/dashboard/matches/:matchId",
  "/dashboard/order-book",
  "/dashboard/programmes",
  "/dashboard/search",
  "/dashboard/settings",
  "/desk",
  "/desk/billing",
  "/desk/compiler/:matchId",
  "/desk/compliance",
  "/desk/data-export",
  "/desk/data-residency",
  "/desk/deals",
  "/desk/deals/:matchId",
  "/desk/discover",
  "/desk/evidence/:id",
  "/desk/facilitation/:id",
  "/desk/facilitation/new",
  "/desk/inbound/review",
  "/desk/inbound/review/:matchId",
  "/desk/match/:matchId",
  "/desk/match/active",
  "/desk/match/new",
  "/desk/match/rejected",
  "/desk/new-trade",
  "/desk/security",
  "/desk/settings",
  "/desk/settings/balance",
  "/desk/settings/company",
  "/desk/settings/notifications",
  "/desk/wizard",
  "/developer",
  "/developer/api/usage",
  "/developer/docs",
  "/developer/keys",
  "/developer/schema",
  "/developer/support",
  "/developer/usage",
  "/developer/webhooks",
  "/developers",
  "/developers/dlq",
  "/developers/docs",
  "/developers/keys",
  "/developers/webhooks",
  "/docs",
  "/docs/api",
  "/docs/authentication",
  "/docs/counterparties",
  "/docs/counterparty-rating-methodology",
  "/docs/errors",
  "/docs/evidence",
  "/docs/legacy",
  "/docs/matches",
  "/docs/quickstart",
  "/docs/sdks",
  "/docs/webhooks",
  "/governance",
  "/governance/audits",
  "/governance/entities",
  "/governance/health",
  "/governance/triage",
  "/hq",
  "/hq/:tab",
  "/landing",
  "/pricing",
  "/products",
  "/products/audit-ledger",
  "/products/compliance-engine",
  "/products/trade-desk",
  "/registry",
  "/registry/claim",
  "/registry/company/:id",
  "/registry/readiness",
  "/registry/search",
  "/reset-password",
  "/solutions",
  "/solutions/finance",
  "/solutions/sovereigns",
  "/solutions/traders",
  "/status",
  "/trade/wizard",
  "/unsubscribe",
  "/walkthrough",
  "/welcome",
  "…",
] as const;

export type RoutePath = "/" | "/admin" | "/admin/api/usage" | "/admin/compliance" | "/admin/data-governance" | "/admin/deals" | "/admin/engagements" | "/admin/entities" | "/admin/orgs" | "/admin/overrides" | "/admin/registry" | "/admin/registry/decisions" | "/admin/registry/readiness" | "/admin/settings" | "/admin/users" | "/auth" | "/billing" | "/dashboard" | "/dashboard/account" | "/dashboard/billing" | "/dashboard/compliance" | "/dashboard/matches" | "/dashboard/matches/:matchId" | "/dashboard/order-book" | "/dashboard/programmes" | "/dashboard/search" | "/dashboard/settings" | "/desk" | "/desk/billing" | "/desk/compiler/:matchId" | "/desk/compliance" | "/desk/data-export" | "/desk/data-residency" | "/desk/deals" | "/desk/deals/:matchId" | "/desk/discover" | "/desk/evidence/:id" | "/desk/facilitation/:id" | "/desk/facilitation/new" | "/desk/inbound/review" | "/desk/inbound/review/:matchId" | "/desk/match/:matchId" | "/desk/match/active" | "/desk/match/new" | "/desk/match/rejected" | "/desk/new-trade" | "/desk/security" | "/desk/settings" | "/desk/settings/balance" | "/desk/settings/company" | "/desk/settings/notifications" | "/desk/wizard" | "/developer" | "/developer/api/usage" | "/developer/docs" | "/developer/keys" | "/developer/schema" | "/developer/support" | "/developer/usage" | "/developer/webhooks" | "/developers" | "/developers/dlq" | "/developers/docs" | "/developers/keys" | "/developers/webhooks" | "/docs" | "/docs/api" | "/docs/authentication" | "/docs/counterparties" | "/docs/counterparty-rating-methodology" | "/docs/errors" | "/docs/evidence" | "/docs/legacy" | "/docs/matches" | "/docs/quickstart" | "/docs/sdks" | "/docs/webhooks" | "/governance" | "/governance/audits" | "/governance/entities" | "/governance/health" | "/governance/triage" | "/hq" | "/hq/:tab" | "/landing" | "/pricing" | "/products" | "/products/audit-ledger" | "/products/compliance-engine" | "/products/trade-desk" | "/registry" | "/registry/claim" | "/registry/company/:id" | "/registry/readiness" | "/registry/search" | "/reset-password" | "/solutions" | "/solutions/finance" | "/solutions/sovereigns" | "/solutions/traders" | "/status" | "/trade/wizard" | "/unsubscribe" | "/walkthrough" | "/welcome" | "…";

/**
 * Runtime guard for paths sourced from data (notification payloads, server
 * responses, etc.). Returns the path narrowed to RoutePath, or throws.
 */
export function assertRoutePath(path: string): RoutePath {
  if ((ROUTE_PATHS as readonly string[]).includes(path)) {
    return path as RoutePath;
  }
  throw new Error(`assertRoutePath: "${path}" is not a registered route. Add it to App.tsx or fix the typo.`);
}

/**
 * Returns true if `path` (with optional ?query) targets a registered
 * route pattern. Used by the route guard's runtime tests.
 */
export function isRoutePath(path: string): path is RoutePath {
  const stripped = path.split("?")[0].split("#")[0];
  return (ROUTE_PATHS as readonly string[]).includes(stripped);
}

/** Options accepted by routeTo(). */
export interface RouteToOptions {
  /** Substitute :param segments. e.g. { matchId: "abc" } turns
   *  "/desk/match/:matchId" into "/desk/match/abc". */
  params?: Record<string, string | number>;
  /** Append a querystring. Values are coerced to strings; undefined is skipped. */
  query?: Record<string, string | number | boolean | undefined | null>;
}

/**
 * Type-safe URL builder.
 *
 * Examples:
 *   routeTo("/desk/settings/company")
 *   routeTo("/desk/settings/company", { query: { step: "entity" } })
 *   routeTo("/desk/match/:matchId", { params: { matchId } })
 *
 * A typo like routeTo("/desk/settings/identity") fails to compile because
 * the literal isn't in the RoutePath union.
 */
export function routeTo(path: RoutePath, opts: RouteToOptions = {}): string {
  let out: string = path;
  if (opts.params) {
    for (const [k, v] of Object.entries(opts.params)) {
      out = out.replace(`:${k}`, encodeURIComponent(String(v)));
    }
  }
  if (opts.query) {
    const usp = new URLSearchParams();
    for (const [k, v] of Object.entries(opts.query)) {
      if (v === undefined || v === null || v === "") continue;
      usp.set(k, String(v));
    }
    const qs = usp.toString();
    if (qs) out += `?${qs}`;
  }
  return out;
}
