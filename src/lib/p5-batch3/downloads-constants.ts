/**
 * P-5 Batch 3 — Stage 5 download UI constants.
 *
 * Mirrors the TTL window defined in src/lib/p5-batch3/downloads.ts. Kept as
 * a tiny presentation-only constant so the funder downloads page does not
 * have to import from the broader downloads logic module.
 */
import { P5B3_DOWNLOAD_LINK_TTL_DAYS } from "./downloads";

export const P5B3DOWNLOAD_LINK_TTL_DAYS_LABEL = `${P5B3_DOWNLOAD_LINK_TTL_DAYS} days`;
